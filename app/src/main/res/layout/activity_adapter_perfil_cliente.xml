<?xml version="1.0" encoding="utf-8"?>

<!--LinearLayour que contiene todos los elementos-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="10dp"
    android:orientation="vertical"
    android:padding="10dp">

    <!--TextView que muestra el titulo del dibujo-->
    <TextView
        android:id="@+id/tituloDibujoPerfilCliente"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/caja_texto"
        android:padding="3dp"
        android:text="TextView"
        android:textAlignment="center"
        android:textColor="@color/negro"
        android:textSize="24sp"
        android:textStyle="bold" />

    <!--ImageView que muestra la imagen-->
    <ImageView
        android:id="@+id/dibujoPerfilCliente"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_margin="5dp"
        android:adjustViewBounds="true"
        android:clickable="false"
        android:cropToPadding="false" />

</LinearLayout>